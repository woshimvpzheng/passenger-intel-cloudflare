{
  "name": "passenger-intelligence",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node dev-server.mjs",
    "cloudflare:dev": "npx wrangler dev",
    "cloudflare:dry-run": "npx wrangler deploy --dry-run",
    "cloudflare:deploy": "npx wrangler deploy",
    "test": "node --test tests/*.test.mjs",
    "refresh": "node scripts/run-refresh.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^9.1.0"
  },
  "devDependencies": {}
}
